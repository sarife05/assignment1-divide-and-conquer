
```bash
git init
git add .gitignore pom.xml
git commit -m "init: project structure and build"
git branch -M main
git remote add origin https://github.com/<user>/assignment1-divide-and-conquer.git
git push -u origin main
```
