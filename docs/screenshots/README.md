# Screenshots


| File | How to produce it |
|---|---|
| `demo-output.png`  | terminal after `mvn exec:java -Dexec.args="demo"` |
| `tests-passed.png` | terminal after `mvn test` (the "Tests run: ... Failures: 0" summary must be visible) |
| `bench-output.png` | terminal after `mvn exec:java -Dexec.args="bench results/results.csv"` |
| `plots.png`        | `docs/plots/time_vs_n.png` and `depth_vs_n.png` open side by side, or the rendered results table |

